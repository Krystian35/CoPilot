# Lab 06 — Generate YAML parameters file from Bicep template

### 🎯 Goal

Use **Copilot Chat** to generate a YAML parameters file for the Bicep template.

---

## ✅ Task

1. Open the Bicep file in this folder (`main.bicep`).
2. Use Copilot Chat to:

   * read and interpret required parameters,
   * generate a `deploy.yaml` file with values for those parameters.

> You are **not writing YAML manually** — describe what you want and let Copilot generate it.

---

## 🔧 Prompts to use

### 1. Understand Bicep parameters

```
/explain Read this Bicep file and list all parameters used, along with expected data types.
```

### 2. Generate YAML parameters file

```
Generate a YAML file named deploy.yaml with sample parameter values for this Bicep template. Include sections: **default**, **virtualNetwork** (array with subnets), and **networkSecurityGroup**.
```

### 3. Optional — custom values

```
Update deploy.yaml and set location to 'westeurope' and add tag environment=dev.
```

---

## ✅ Success criteria

* YAML file (`deploy.yaml`) exists.
* YAML contains values for:

  * virtualNetwork
  * networkSecurityGroup
  * default
* No manual YAML formatting — generated by Copilot.

---
